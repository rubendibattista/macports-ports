# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0
PortGroup           python 1.0
PortGroup           qt5 1.0

set real_name       pivy
github.setup        FreeCAD ${real_name} 0.6.5a4
name                py-${real_name}
license             Permissive
maintainers         {mps @Schamschula} openmaintainer
description         Pivy is a Coin binding for Python.
long_description    ${description}
platforms           darwin

python.versions     27 35 36 37

checksums           rmd160  e8f564cd99da93b74825dc952e23c7f7c33168d6 \
                    sha256  1ee77d4a8cb0f064faad5e2624fdceaa34e090aa2328c758d1341666a81b0e9a \
                    size    6614326

qt5.depends_component qtbase

if {${name} ne ${subport}} {
    depends_lib-append \
                    port:Coin-framework \
                    port:swig-python

    patchfiles      patch-setup.py.diff

    post-patch {
        reinplace "s|%QMAKE_COMMAND%|\"${qt_qmake_cmd}\"|g" ${worksrcpath}/setup.py
        
        # # See https://github.com/FreeCAD/pivy/issues/42
        # set fake_headers {cstddef cstdarg cassert}
        # foreach header $fake_headers {
        #     touch ${worksrcpath}/fake_headers/${header}
        # }
    }

    livecheck.type  none
}
